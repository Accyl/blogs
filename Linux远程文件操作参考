1.HTTP文件下载:
  wget 支持文件的断点续传等常规文件下载操作，代理需要通过shell的环境变量设置
  curl 支持文件下载，不支持断点续传等高级特性，代理可以通过自身参数来控制

2.远程主机文件下载:
  scp 支持通过ssh连接远程主机拷贝文件，可以进行文件的上传和下载，不支持代理

3.远程主机文件操作:
  sftp 支持文件复杂操作，上传下载、权限设置等，不支持代理
